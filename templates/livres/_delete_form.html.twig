<form method="post" action="{{ path('app_livres_delete', {'id': livre.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ livre.id) }}">
    <button id="btn-supp-crud" class="btn mt-2">SUPPRIMER</button>
</form>
